import 'package:flutter/material.dart';

class FavoriteScreen extends StatelessWidget {
  static const routeName = '/favoriten';
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('FavoriteScreen'));
  }
}
